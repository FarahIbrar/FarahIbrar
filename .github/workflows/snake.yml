name: Generate snake animation

on:
  schedule: 
    - cron: "0 */12 * * *" # execute every 12 hours

  workflow_dispatch:

  push:
    branches:
    - main

jobs:
  generate:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes
